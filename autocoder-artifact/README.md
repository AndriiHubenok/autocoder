To use the weather analyzer tool, simply call the analyze_data function in the weather_analyzer.py script with the file path of your CSV data. Ensure that the CSV file contains a column named 'Temperature'. The script will output the mean, median, and standard deviation of the temperature data along with a line plot saved as trends.png. Make sure to install the required packages listed in requirements.txt before running the script.
